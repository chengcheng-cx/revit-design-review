<Window x:Class="RevitDesignReview.Revit2026.QuickReviewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Create Review"
        Width="520"
        SizeToContent="Height"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False">
    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock Text="What needs review?" FontWeight="SemiBold" Margin="0,0,0,8" />
        <TextBox x:Name="TitleTextBox"
                 Grid.Row="1"
                 MinHeight="64"
                 TextWrapping="Wrap"
                 AcceptsReturn="True"
                 VerticalScrollBarVisibility="Auto" />
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,14,0,0">
            <Button Content="Cancel" Width="82" Margin="0,0,8,0" IsCancel="True" />
            <Button Content="Create" Width="82" IsDefault="True" Click="CreateClick" />
        </StackPanel>
    </Grid>
</Window>
